'''
  Code a three-line function that creates a list with three integers.
  Pass the list back to the calling code. Make everything up.
'''

def listCreator(a, b, c):
  list = [a, b, c]
  return list

print(listCreator(3, 4, 5))
