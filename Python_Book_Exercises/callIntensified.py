'''
    Code a function that calls this function,
    displaying the results.
    Call the function you coded, passing three
    arguments that are numbers.
'''

def calc(x, y, z):
  return x * y * z

def callCalc(u, v, w):
 return (calc(u, v, w))
  
  
print(callCalc(2, 3, 4))
