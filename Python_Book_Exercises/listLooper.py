'''
    Code a function that loops through a result
    returned by a function and appends each
    returned value to a list.
    The outer function has one positional parameter,
    the name of the list. Make everything up.

'''

def loopFunct(list):
  for i in retRes():
    list.append(i)
