'''
  Code a function that performs a calculation
  on its two parameters and passes the result back
  Call the function, passing two positional
  arguments that are integers.
  Display the result produced by the function call.
  There are two opportunities here to condense your code.
  Take them if you wish.
'''

def mod(i, j):
  return i % j

print(mod(9, 7))
