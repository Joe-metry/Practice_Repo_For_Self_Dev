'''
  Code the first two lines of a block with exception handling.
  The second line opens a JSON file for writing.
  Assume the json module has already been imported.
  Make up what you need to.
'''

try:
  with open("theJsonfile.json", "w") as J:
