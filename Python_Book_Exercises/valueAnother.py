''
As long as a variable holding an integer is less than the value of another
variable, run a for loop that loops through all the elements of a list and
adds each element of the list to the first variable. Use the concise approach
to increment the first variable. Make everything up.
'''

while var < rav::
  for i in theList:
    var += i
