'''
Call a function, assigning its returned value to a variable. 
he function's argument is another function with one
positional argument, a string. Make everything up.

'''

retVal = calledFunc(anotherFunct("Literal Data"))
