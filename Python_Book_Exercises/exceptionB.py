'''
Code the next line to deal with the file not being found.
try:
  with open("xyz.json", "w") as j:
'''
except FileNotFoundError:

'''
  Code the first two lines of a block with exception handling.
  The second line opens a text file for reading.
  Use shorthand. Make up what you need to.
'''

try:
  with open("the_file.txt") as J:
