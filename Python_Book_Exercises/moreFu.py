'''
    Code a function that has two parameters that are lists.
    As long as the first list is shorter than the second list,
    append an element, "empty", to the first list.
    Make up the names of the function and variables.
'''

def twoPara(listA, listY):
  while len(listA) < len(listY):
  listA.append("empty")

